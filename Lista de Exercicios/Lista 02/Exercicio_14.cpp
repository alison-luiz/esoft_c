// Faça um programa que apresente todos os números inteiros entre 12 e 25, na horizontal

#include <stdio.h>

int main() {
    int num = 12;
    while (num <= 25) {
        printf("%d ", num);
        num++;
    }
    return 0;
}
